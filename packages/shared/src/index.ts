/**
 * @raah/shared — cross-cutting utilities: env (Zod-validated), dates (Luxon, IST-aware),
 * money (INR lakh/crore), Result/AppError taxonomy, SSE event schemas, typed feature flags.
 * Modules land in P0.4/P0.8/P0.15.
 */
export const SHARED_PACKAGE = "@raah/shared" as const;
