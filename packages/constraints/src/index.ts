/**
 * @raah/constraints — deterministic rule layer (ARCH §7.3).
 * Season windows, permits, closures, rail booking-window math, altitude acclimatization,
 * pacing, budget arithmetic. Pure TypeScript, ZERO LLM calls (CI-enforced).
 *
 * Implemented in Phase 1 (P1.7–P1.8). Placeholder export until then.
 */
export const CONSTRAINTS_PACKAGE = "@raah/constraints" as const;
