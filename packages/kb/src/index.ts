/**
 * @raah/kb — India Knowledge Base: content-repo loaders, hybrid retrieval, freshness (ARCH §9).
 * Deterministic facts (closures, permits) via exact lookup — never vector-fuzzy.
 *
 * Implemented in Phase 2. Placeholder export until then.
 */
export const KB_PACKAGE = "@raah/kb" as const;
